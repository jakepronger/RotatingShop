time:
  uptime: 329 # tracks current rotation time in minutes (resets every rotation)
  uptime-updater: # update uptime every specified minutes in-case of an in proper server shutdown (crash)
    use: true
    minutes: 5

# todo: update these id's if an item is removed
rotation: 1,3,5

items: